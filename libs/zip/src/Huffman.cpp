#include "zip_stdafx.hpp"

namespace zip
{
  Huffman::Huffman()
  {
  }

  std::unique_ptr<std::uint8_t[]> Huffman::compress(std::uint8_t const *data,
                                                    std::size_t         len)
  {
    return (nullptr);
  }

  std::unique_ptr<std::uint8_t[]> decompress(std::uint8_t const *data,
                                             std::size_t         len)
  {
    return (nullptr);
  }
}